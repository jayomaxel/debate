[pytest]
markers =
    integration: slower integration tests that rely on external services or databases
    pgvector: requires PostgreSQL test database with pgvector configured via TEST_PGVECTOR_DATABASE_URL
    e2e: production-stack release gate using PostgreSQL, pgvector, Redis and real filesystem dependencies
