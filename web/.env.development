# Leave API / WS empty in development when the frontend is served locally.
# Vite will proxy /api, /ws and /uploads to the local backend.
#VITE_API_BASE_URL=
#VITE_WS_BASE_URL=
VITE_DEV_API_PROXY_TARGET=http://localhost:7861
VITE_DEV_WS_PROXY_TARGET=ws://localhost:7861
VITE_DEV_SERVER_PORT=5173
