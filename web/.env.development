# Leave API / WS empty in development when the frontend is served to other
# devices through LAN or cpolar. Vite will proxy /api, /ws and /uploads.
#VITE_API_BASE_URL=
#VITE_WS_BASE_URL=
VITE_DEV_SERVER_PORT=5173
