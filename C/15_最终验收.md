# 最终验收

## 本轮目标

检查工作包 C 的改动是否符合使用逻辑、是否真实跑通、是否存在只改文档不落功能的假修改，并记录最后的风险状态。

## 修改文件

- `api/tests/test_rate_limit.py`
- `C/README.md`
- `C/15_最终验收.md`

## 代码逻辑

- 对 `api/tests/test_rate_limit.py` 做了测试稳定性修正：
  - 固定限流测试时间窗，避免碰到分钟边界时偶发跨窗。
  - 强制测试走内存限流后端，避免被本机 Redis 环境干扰。
- 在 `C/README.md` 增加最终验收文档索引。
- 新增本文件，沉淀最后一轮检查结果。

## 对应功能

- 限流测试结果变得可重复，能真实反映 `RateLimitMiddleware` 是否工作，而不是被时间窗抖动误伤。
- 工作包 C 的验收结果可以直接在仓库里复查，不依赖聊天记录。

## 验收命令与结果

### 1. Docker Compose 配置检查

执行：

```bash
docker compose -f docker-compose.yml config
docker compose -f docker-compose.dev.yml config
```

结果：

- 两套 compose 均能成功展开。
- 本机 Docker 读取用户级 `C:\Users\mao\.docker\config.json` 时有权限警告，但不影响 `config` 输出。
- 生产配置已包含 `db`、`redis`、`api`、`web` 四个服务。
- 开发配置已去掉 `network_mode: host`，并加入 Vite 代理环境变量。

### 2. 后端测试

执行：

```bash
python -m pytest tests/test_upload_guard.py tests/test_audit_service.py tests/test_rate_limit.py tests/test_auth_session.py tests/test_websocket_ticket.py tests/test_config_masking.py tests/test_health_check.py -q
```

结果：

- `44 passed`

结论：

- 登录/刷新冻结合同
- 服务端会话吊销
- WebSocket ticket
- 配置脱敏
- 上传安全网关
- 审计日志
- 基础限流
- health/metrics

以上关键能力都有实际测试覆盖，不是纯文档或假接线。

### 3. 前端依赖、类型检查和构建

执行：

```bash
pnpm install --frozen-lockfile
pnpm type-check
pnpm build
```

结果：

- 依赖安装成功
- `type-check` 通过
- `build` 通过

说明：

- 构建产物已正常生成到 `web/dist`
- 构建时有大 chunk 警告，但不阻塞当前交付

### 4. 额外检查

执行：

```bash
git status --short
```

结果：

- 变更集中在工作包 C owner 文件与新增文档范围内
- 未发现越界修改 `web/src/**`、`teacher.py`、`student.py`、`debate_service.py`、`assessment_service.py`

## 是否有假修改

结论：当前未发现假修改。

依据：

- 上传、安全、审计、限流、metrics 都有真实代码入口和测试覆盖。
- 容器、CI、运维文档都已经落到仓库，而不是停留在说明层。
- 前端类型检查与生产构建通过，说明 `Dockerfile.web` 改成 `pnpm build` 后仍可工作。

## 风险与遗留项

- 本机 Docker daemon 当前未运行，因此没有完成 `nginx -t` 或整套容器实际 `up` 的运行态验证。
- 后端测试存在一批已有的 deprecation warning：
  - Pydantic class-based config
  - FastAPI `on_event`
  - `datetime.utcnow()`
- 前端构建存在大 chunk 警告，后续可以再做拆包优化。

## 最终结论

工作包 C 当前可以判定为已完成。

阻塞项状态：

- 功能实现：完成
- 文档沉淀：完成
- 自动化验证：完成
- 容器运行态验证：受本机 Docker daemon 未启动影响，未做最后一步动态启动验证，但静态配置检查已通过
